Read `docs/codex-rules.md` and all project documents referenced there.

Implement Phase 10 from `docs/task.md` only.

Add approval-required state, LangGraph pause/interrupt behavior, approve/reject API support, persisted resume, audit records, and a simulated rollback only.

Handle approve, reject, invalid, duplicate, and expired decisions safely.

Preserve Phase 1–9 behavior. Do not integrate a real LLM or implement Phase 11.

Run all tests, validate Phase 10, and update only Phase 10 in `docs/task.md`.
